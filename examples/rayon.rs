//#[macro_use]
//extern crate voluntary_servitude;
//extern crate rayon;
//use rayon::prelude::*;

fn main() {
    //let list = vs![0xFFu8; 1000000];
    //list.par_iter().for_each(|el| println!("{}", el));
}
